#include<stdio.h>

struct _complex
 {
    float real,imaginary;
};


typedef struct _complex Complex;
struct _complex a;
Complex a;

 int input()
{
  int n;
  printf("how manycomplex numbers\n");
  scanf("%d",&n);
  return n;
}

Complex input_complex()
{
  Complex c;
  printf("enter the real number and imaginary parts of complex number\n");
  scanf("%f %f ",&c.real,&c.imaginary);
  return c;

}
void input_n_complex(int n;Complex c[n])
{
  for(int i=0;i<n;i++)
  {
    printf("Enter the real and imaginary value of Complex number\n");
    Scanf("%f%f,&c[i].real,&c[i].imaginary");
  
  }
}

Complex add_complex(Complex c1,Complex c2)
{
  Complex c3;
  c3.real=c1.real+c2.real;
  c3.imaginary=c1.imaginary+c2.imaginary;
  return c3;

}

Complex add_n_complex(int n,Complex c[n])
{
  Complex sum = {0,0};
  for(int i=0;i<n;i++)
  {
    sum= add_n_complex(sum,c[i]);
  }
  return sum;
}

void output(int n,Complex c[n],Complex sum)
{
  for(int i=0;i<n-1;i++)
  printf("(%f+%fi)+\n",c[i].real,c[i].imaginary);
  printf("(%f+%fi)+\n",c[n-1].real,c[n-1].imaginary);
  printf("(%f+%fi)+\n",sum.real,sum.imaginary);
}
int main()
{
  int n;
  n=input();
  Complex c[n];
  input_n(n,c);
  Complex sum=add_n_complex(n,sum);
  output(n,c,sum);
  return 0;
}